# Backend Selection Policy Stream

id: stream-backend-selection-policy
status: planned
objective: Define backend selection policy per platform/architecture (OpenGL/Metal/etc) with deterministic validation evidence.

## Expected Deliverables

- ADR covering policy and tradeoffs.
- Runtime override mechanism contract (`env` + config).
- Platform capability matrix updates with before/after evidence.

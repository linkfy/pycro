# Phase 13-cli-generate-stubs-command - CLI Generate Stubs Command

status: planned
objective: Expose stub regeneration through `pycro generate_stubs` without requiring direct `cargo run --bin generate_stubs` usage.
tracked_tasks: cli-generate-stubs-subcommand

## Links

- `docs/task-tracker.txt`
- `state/repo-state.json`

# Requirements

phase_objective: Run a real Python script through runtime and desktop loop.

## Acceptance Criteria
- Runtime loads script and dispatches setup/update.
- Backend owns desktop frame loop.
- Basic API calls work through real execution path.

## Constraints

- Requirements must stay synchronized with docs/task-tracker.txt and state/repo-state.json.
- If criteria change, update interactive-refinement.md before implementation continues.

# Requirements

phase_objective: Resolve entry-script sidecar imports reliably.

## Acceptance Criteria
- main.py imports sidecar modules from script directory.
- Import precedence remains deterministic.
- Lifecycle behavior remains stable.

## Constraints

- Requirements must stay synchronized with docs/task-tracker.txt and state/repo-state.json.
- If criteria change, update interactive-refinement.md before implementation continues.
